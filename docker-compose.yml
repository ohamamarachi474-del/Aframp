version: '3.8'

services:
  aframp-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: aframp-app
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - aframp-network

networks:
  aframp-network:
    driver: bridge
